# Core dependencies for geometry diagram generator
python-dotenv>=1.0.0

# AI APIs
google-genai>=1.0.0      # Gemini Flash (classify + blueprint)
openai>=1.0.0            # DeepSeek V3.2 via Azure endpoint

# Web UI (batch testing)
flask>=3.0.0
